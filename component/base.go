package component

// Base implements as a base of a component
type Base struct {
	// Use Component Interface
}

// Init initializes the component
func (b *Base) Init() { /* No implement */ }

// Shutdown shuts down the component
func (b *Base) Shutdown() { /* No implement */ }
